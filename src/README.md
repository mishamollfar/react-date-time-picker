use DateTimePicker component with parameter:
    - title - name button where empty date;
    - onChangeDateTime - callback for selected date-time

```
    <DateTimePicker title={'select date-time'} onChangeDateTime={this.changeDateTime}/>
```
